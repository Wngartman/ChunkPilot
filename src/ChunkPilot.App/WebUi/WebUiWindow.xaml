<Window x:Class="ChunkPilot.App.WebUi.WebUiWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="ChunkPilot"
        Width="1280" Height="820" MinWidth="920" MinHeight="620"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource AppSurfaceCanvas}"
        Foreground="{DynamicResource AppTextPrimary}">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="6" CornerRadius="0" GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>
    <Grid Background="{DynamicResource AppSurfaceCanvas}">
        <wv2:WebView2 x:Name="Browser" />
        <Border x:Name="FailureSurface" Visibility="Collapsed" Background="{DynamicResource AppSurfaceCanvas}" Padding="48">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="560">
                <TextBlock Text="ChunkPilot could not start" Style="{StaticResource AppTitleLargeText}" Margin="0,0,0,12" />
                <TextBlock x:Name="FailureDetail" Style="{StaticResource AppSecondaryText}" />
                <WrapPanel Margin="0,24,0,0">
                    <Button x:Name="RetryButton" Content="Retry" Click="RetryButton_OnClick" Style="{StaticResource AppPrimaryButton}" Margin="0,0,10,10" />
                    <Button Content="Repair or install WebView2" Click="RepairWebViewButton_OnClick" Margin="0,0,10,10" Padding="18,9" />
                    <Button Content="Open diagnostics" Click="OpenDiagnosticsButton_OnClick" Margin="0,0,10,10" Padding="18,9" />
                    <Button Content="Exit" Click="CloseButton_OnClick" Margin="0,0,0,10" Padding="18,9" />
                </WrapPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
